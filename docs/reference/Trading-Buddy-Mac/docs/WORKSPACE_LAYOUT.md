# Suggested Workspace Layout

```
merge_lab/
  signal_bus/
  gateway/
  freqtrade_body/
  tradegate_adapter/
  reports/
```

## Notes
- Keep all merged system work inside `merge_lab/` (or similar).
- Do not alter the live `/Users/utlyze/clawd/tradegate` until after the 10‑hour run completes.

